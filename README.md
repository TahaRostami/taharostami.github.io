# Run
1. cd src
2. hugo server --minify --theme book

# Deploy
1. cd src
3. hugo -D --minify --theme book
4. copy all files in the 'public' folder and paste them into home directory

** Note ** don't remove src, README.md and .git when make changes 

